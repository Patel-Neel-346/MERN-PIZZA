# Project identification
sonar.projectKey=Patel-Neel-346_MERN-PIZZA
sonar.organization=patel-neel-346
sonar.projectName=MERN-PIZZA

# Source code directory
sonar.sources=src

# Optional: test directory (not strictly required)
# sonar.tests=test

# Coverage report path
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Exclude folders that are NOT source code
sonar.exclusions=**/node_modules/**,**/dist/**,**/build/**,**/migration/**,**/coverage/**,**/coverage/lcov-report/**

# Wait for quality gate before ending scan
sonar.qualitygate.wait=true

# Disable SCM exclusion (you already set this correctly)
sonar.scm.exclusions.disabled=true
